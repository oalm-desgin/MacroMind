apiVersion: v1
kind: Service
metadata:
  name: nutrition-ai-service
  namespace: macromind
  labels:
    app: nutrition-ai-service
spec:
  type: ClusterIP
  ports:
  - port: 8002
    targetPort: 8002
    protocol: TCP
    name: http
  selector:
    app: nutrition-ai-service

