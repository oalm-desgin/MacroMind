# MacroMind Docker Compose Environment Variables Template
# Copy this file to .env and update with your actual values

# PostgreSQL Database Configuration
POSTGRES_USER=macromind
POSTGRES_PASSWORD=changeme_secure_password_here
POSTGRES_DB=macromind

# Database Connection URL (uses postgres container hostname)
# Format: postgresql://user:password@host:port/database
DATABASE_URL=postgresql://macromind:changeme_secure_password_here@postgres:5432/macromind

# JWT Secret Key (REQUIRED for auth-service and token validation)
# Generate a secure random key: openssl rand -hex 32
JWT_SECRET_KEY=changeme_generate_secure_jwt_secret_key_here

# OpenAI API Key (REQUIRED for nutrition-ai-service and meal-planner-service)
# Get your API key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=changeme_your_openai_api_key_here

# Optional: JWT Algorithm (defaults to HS256 if not set)
# JWT_ALGORITHM=HS256

# Optional: Token Expiration (defaults shown)
# JWT_ACCESS_TOKEN_EXPIRE_MINUTES=30
# JWT_REFRESH_TOKEN_EXPIRE_DAYS=7

# Optional: CORS Origins (comma-separated)
# CORS_ORIGINS=http://localhost:3000,http://localhost:5173

# Optional: Environment (production/development)
# ENVIRONMENT=production
