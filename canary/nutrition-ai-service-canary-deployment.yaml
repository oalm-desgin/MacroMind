apiVersion: apps/v1
kind: Deployment
metadata:
  name: nutrition-ai-service-canary
  namespace: macromind
  labels:
    app: nutrition-ai-service
    version: canary
spec:
  replicas: 1
  selector:
    matchLabels:
      app: nutrition-ai-service
      version: canary
  template:
    metadata:
      labels:
        app: nutrition-ai-service
        version: canary
    spec:
      containers:
      - name: nutrition-ai-service
        image: macromind/nutrition-ai-service:canary  # Update with your image
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 8002
          name: http
        env:
        - name: DATABASE_URL
          valueFrom:
            secretKeyRef:
              name: macromind-secrets
              key: database-url
        - name: JWT_SECRET_KEY
          valueFrom:
            secretKeyRef:
              name: macromind-secrets
              key: jwt-secret
        - name: OPENAI_API_KEY
          valueFrom:
            secretKeyRef:
              name: macromind-secrets
              key: openai-api-key
        - name: CORS_ORIGINS
          value: "http://localhost:3000,http://localhost:5173,http://localhost:5174"
        resources:
          requests:
            cpu: 200m
            memory: 512Mi
          limits:
            cpu: 1000m
            memory: 1Gi
        livenessProbe:
          httpGet:
            path: /health
            port: 8002
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: 8002
          initialDelaySeconds: 10
          periodSeconds: 5

---
apiVersion: v1
kind: Service
metadata:
  name: nutrition-ai-service-canary
  namespace: macromind
  labels:
    app: nutrition-ai-service
    version: canary
spec:
  type: ClusterIP
  ports:
  - port: 8002
    targetPort: 8002
    protocol: TCP
    name: http
  selector:
    app: nutrition-ai-service
    version: canary

